#include "SortBed.hpp"

#include <set>

using namespace std;

SortBed::SortBed(std::istream& in, std::ostream& out)
    : _in(in)
    , _out(out)
{
}

void SortBed::exec() {
    string line;
    while (_in.good() && !_in.eof()) {

       
    }
}
